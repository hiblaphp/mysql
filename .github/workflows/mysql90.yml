name: MySQL 9.0 Tests

on: [push, pull_request]

jobs:
  mysql90-ci:
    name: PHP ${{ matrix.php }} - MySQL 9+
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      matrix:
        php: ['8.4', '8.5']

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          tools: composer:v2
          coverage: none
          extensions: openssl, zlib, bcmath, mbstring

      - name: Cache Composer dependencies
        uses: actions/cache@v3
        with:
          path: ~/.composer/cache/files
          key: dependencies-php-${{ matrix.php }}-composer-${{ hashFiles('composer.json') }}
          restore-keys: |
            dependencies-php-${{ matrix.php }}-composer-

      - name: Install Dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Start MySQL 9.0 Services
        run: docker compose up -d mysql90 mysql90_ssl 

      - name: Wait for MySQL 9.0 Health
        run: |
          echo "Waiting for MySQL 9.0 containers to be healthy..."
          ATTEMPTS=0
          until [ $ATTEMPTS -eq 30 ] || [ $(docker ps | grep -c "(healthy)") -eq 2 ]; do
            sleep 2
            ATTEMPTS=$((ATTEMPTS+1))
          done
          docker ps

      - name: Run Tests
        run: composer test:mysql90

      - name: Shutdown Services
        if: always()
        run: docker compose down -v